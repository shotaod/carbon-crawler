spring:
  output.ansi.enabled: ALWAYS
  batch:
    initialize-schema: always

carbon:
  rdb:
    rdbUrl: jdbc:h2:mem:crawler_definition
    rdbUsername: carbon
    rdbPassword: carbonpw
    rdbOption:
      useSSL: false
  wd:
    driverHubUrl: http://driver.carbon-crawler.local:4444/wd/hub