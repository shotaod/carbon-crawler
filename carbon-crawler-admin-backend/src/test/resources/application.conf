test {
  carbon.crawler {
    server {
      port = 9001
      watch = ["carbon-crawler-admin-backend"]
    }
    db {
      host = rdb.carbon-crawler.local
      host = ${?CARBON_TEST_RDB_HOST}
      port = 43306
      port = ${?CARBON_TEST_RDB_PORT}
      schema = crawler_definition
      schema = ${?CARBON_TEST_RDB_SCHEMA}
      username = carbon
      username = ${?CARBON_TEST_RDB_USER}
      password = carbonpw
      password = ${?CARBON_TEST_RDB_PASSWORD}
      option = {
        useSSL = false,
      }
    }
  }
}
