spring.cloud.config:
  uri: http://${CARBON_API_CONFIG_HOST:config.carbon.local}:${CARBON_API_CONFIG_PORT:40000}
  fail-fast: true
  retry:
spring:
  cloud:
    config:
      retry:
        max-attempts: 30
